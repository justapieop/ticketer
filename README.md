# ticketer

CLI tools written in Rust to manage tickets

# commands

Create a ticket

```bash
create (title) (subject)
```

List

```bash
list
```

Get a ticket

```bash
get (id)
```

Edit a ticket

```bash
edit (id)
```

Exit the app


```bash
exit
```